# Optional: override the SQLite file location (defaults to backend/data/bhatta.db)
# SQLITE_PATH="/var/data/bhatta.db"
PORT=4000
JWT_SECRET="change-this-in-production"
CORS_ORIGIN="http://localhost:5173"
